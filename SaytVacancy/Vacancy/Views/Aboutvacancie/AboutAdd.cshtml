@using Vacancy.Core
@using Vacancy.Repository.Dto.AboutvacancieDto


@model AboutCreateDto

@{
    var aboutvacancie = ViewBag.Aboutvacancies as IEnumerable<Aboutvacancie>;
        
}

<div class="container-fluid pt-4 px-4">
                <div class="row g-4">
                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-light rounded h-100 p-4">
    <form asp-action="Vacancy" class="col-md-4">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @Html.AntiForgeryToken()    
        
        <div class="form-group">
            <label asp-for="AboutvacancieName" class="control-label">Про Вакансії</label>
            <select asp-for="AboutvacancieName" name="aboutvacancieName" id="aboutvacancieName" class="form-select" required>
                <option selected disabled>Choose...</option>
                @foreach (var u in aboutvacancie)
                {
                    <option value="@u.AboutvacancieName">@u.AboutvacancieName</option>
                }
            </select>
            <span asp-validation-for="AboutvacancieName" class="text-danger"></span>
        </div>       

<div class="form-group">
                       
        <hr />

        <div class="form-group">
            <button value="Create" va class="btn btn-primary" type="submit">Додати</button>
        </div>
    </form>
</div>



@section Scripts {
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(function () {
            $(".form-select").select2();
        });
    </script>
}